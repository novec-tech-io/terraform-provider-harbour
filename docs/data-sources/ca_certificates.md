---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "harbour_ca_certificates Data Source - harbour"
subcategory: ""
description: |-
  Reads Harbour's CA certificates for trust-store distribution. Anchor trust on the root certificate. The list attributes contain a single element today; during a CA rotation or a bring-your-own-root migration window they may contain two overlapping anchors.
---

# harbour_ca_certificates (Data Source)

Reads Harbour's CA certificates for trust-store distribution. Anchor trust on the root certificate. The list attributes contain a single element today; during a CA rotation or a bring-your-own-root migration window they may contain two overlapping anchors.

## Example Usage

```terraform
data "harbour_ca_certificates" "this" {}

# Anchor trust on the root certificate — write it to a local trust-store file...
resource "local_file" "harbour_root_ca" {
  content         = data.harbour_ca_certificates.this.root_pem
  filename        = "/etc/pki/ca-trust/source/anchors/harbour-root-ca.pem"
  file_permission = "0644"
}

# ...or distribute it via SSM for instances to pull at boot.
resource "aws_ssm_parameter" "harbour_root_ca" {
  name  = "/pki/harbour/root-ca"
  type  = "String"
  value = data.harbour_ca_certificates.this.root_pem
}

output "chain_pem" {
  description = "Full CA chain (intermediate first, root last)"
  value       = data.harbour_ca_certificates.this.chain_pem
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `chain_pem` (String) The full CA chain as concatenated PEM, intermediate first, root last.
- `intermediate_pem` (String) The first intermediate CA certificate, as PEM. Convenience scalar for the common single-intermediate case.
- `intermediates` (List of String) PEM-encoded intermediate CA certificates.
- `root_pem` (String) The first root CA certificate, as PEM. Convenience scalar for the common single-root case.
- `roots` (List of String) PEM-encoded root CA certificates. Install these in trust stores.
